spring:
  datasource:
    url: jdbc:h2:mem:pulse-demo
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  h2:
    console:
      enabled: true

youtube:
  api-key: demo

anthropic:
  api-key: demo
  model: claude-sonnet-4-20250514

pulse:
  collector:
    enabled: false
  analyzer:
    enabled: false
  demo:
    load-data: true
